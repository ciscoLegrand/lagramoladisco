<p id="notice"><%= notice %></p>

      <h4 class="panel-heading | subtitle is-4 | has-text-centered">
       Premios
      </h4>

  <div class="card-content">
    <div class="b-table has-pagination">
      <%= link_to 'New Award', new_award_path, class: 'button is-primary is-small' %>
      <div class="table-wrapper has-mobile-cards">
        <table class="table is-fullwidth is-striped is-hoverable is-sortable is-fullwidth">
          <thead>
            <tr>
              <th class="title is-5">imagen</th>
              <th class="title is-5">Descripción</th>
              <th colspan="3"></th>
            </tr>
          </thead>

          <tbody>
              <% if @awards.present? %>
              <tr>
                <td>
                  <% if @awards.images.attached? %>
                    <%= image_tag url_for(@awards.images.first), data: {src: "#{rails_blob_url(@awards.images.first)}"}, width: '50' %>
                  <% end %>
                </td>
                <td><%= @awards.body %></td>
                
                <td><%= link_to svg('ico-eye.svg'), @awards, class: 'icon is-centered is-vcentered' %></td>
                <td><%= link_to svg('ico-edit.svg'), edit_award_path(@awards), class: 'icon is-centered is-vcentered' %></td>
                <td><%= link_to svg('ico-trash.svg'), @awards, method: :delete, data: { confirm: 'Are you sure?' }, class: 'icon mdi mdi-light is-centered is-vcentered' %></td>
              </tr>
              <% end %>
          </tbody>
        </table>

      </div>
    </div>
  </div>

